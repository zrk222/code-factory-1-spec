# AI Workflow Rules

- One task packet per session; hard context reset between tasks (Ralph Wiggum Loop).
- Read ONLY packet-listed files (R_f minimization).
- Every task ends with its verification command passing.
- If the spec is ambiguous: STOP and emit a question, do not guess.
